<td style="text-align: {{ align }}">
    {% if link %}
    <a href="{{ link }}" class="text-primary">
        {% endif %}

        <span class="{{ classes }}">{{ text }}</span>

        {% if link %}
    </a>
    {% endif %}
</td>